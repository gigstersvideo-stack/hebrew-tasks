# Roadmap — Ивритский тренажёр

Список того, что ЕЩЁ НЕ сделано — в отличие от CHANGELOG.md, где то, что уже
сделано. Проверяй этот файл в начале работы, обновляй по ходу (вычёркивай
сделанное — перенося смысл в CHANGELOG, не дублируя тут "уже готово"),
дописывай новое, когда всплывает идея, которую не делаем прямо сейчас.

Файлы планов из `/plan` (`~/.claude/plans/*.md`) сюда не переносятся
автоматически и могут устареть незаметно — если план оттуда упоминается,
сверяй с реальным кодом/CHANGELOG, прежде чем предлагать его как ещё не
сделанный.

См. также [ROADMAP.md читалки](https://github.com/gigstersvideo-stack/hebrew-reader/blob/main/ROADMAP.md)
— сайты связаны, часть идей может касаться обоих сразу.

Отзывы с сайта разбираются в [FEEDBACK_LOG.md](FEEDBACK_LOG.md) —
сверяйся с ним при новой выгрузке отзывов, прежде чем разбирать их заново
(многие уже были устаревшими на момент отправки).

## Открытые пункты

- **Курс "Корни" — честность написания и качество данных** (обновлено
  2026-09-14) — сама курс-UI полностью готова и встроена (все 200
  корней, экран-список, навигация, sync между устройствами — см.
  CHANGELOG). Открытым остаётся именно КАЧЕСТВО сгенерированного
  контента: (1) удвоение слабой корневой буквы ו/י по всему корпусу
  (было 152 нарушения, чиню постепенно из-за суточной квоты Gemini —
  сейчас 141); (2) та же порча (чужой алфавит внутри слова) в
  растущих текстах, ~201 место на 29 из 66 чекпоинтов, плюс потребуется
  переозвучка исправленных мест; (3) 7 предложений, где `cloze_token`
  не совпадает буквально с текстом предложения — упражнение «пропуск
  слова» для них сломано. Подробности по механике генерации —
  ROOTS_CURRICULUM.md, по самим находкам — FEEDBACK_LOG.md.
- **Шэдоуинг** (2026-09-09, из отзыва с сайта) — новый тип упражнения
  «повторить вслух за диктором». Нужно сперва решить формат: с записью
  голоса пользователя и проверкой произношения (тяжело) или просто
  повтор вслух без обратной связи (намного проще) — от этого зависит
  оценка объёма работы.
- **Направленная озвучка карточек** (2026-09-09, из отзыва с сайта) —
  если карточка иврит→русский, проговаривать при показе (аудирование);
  если русский→иврит — после ввода ответа (подтверждение произношения).
  Разумный педагогический запрос, но упирается в браузерные
  autoplay-политики и конфликтует с уже существующим режимом
  «Аудирование» — решили не делать наспех, нужен отдельный дизайн.
- **Разбор песен на иврите** (2026-09-11) — идея, детали пока не
  обсуждали: какой формат разбора, куда встраивается (тренажёр/читалка),
  откуда брать тексты песен (авторские права — вопрос, который стоит
  продумать до старта, в отличие от собственных сгенерированных
  историй). Ждёт обсуждения.
- **Английский вариант сайта** (2026-09-11) — идея, вероятно касается
  обоих сайтов сразу (тренажёр + читалка связаны), детали (полная
  локализация интерфейса vs просто английские версии карточек/переводов,
  отдельный домен/путь vs переключатель языка) не обсуждали. Ждёт
  обсуждения. См. также пункт в ROADMAP.md читалки.
- Рост библиотеки контента у читалки, см. её ROADMAP.md по ссылке выше.

## Пункты, закрытые после ✓

- ~~Курс "Корни" — встроить оставшиеся 199 корней~~ — встроено полностью
  (все 200 корней, экран-список, навигация, sync между устройствами),
  обкатано вживую. Осталась только чистка качества данных — см. новый
  пункт выше.
- ~~Аналитика ни на одном сайте~~ — Cloudflare Web Analytics подключена
  на обоих сайтах (тренажёр v1.20.0, читалка v1.52.0, 2026-09-10).
- ~~`test_harness.js` не закоммичен в git~~ — закоммичен (2026-09-10), 66
  тестов, self-extracting прямо из задеплоенного `index.html` (там уже
  настоящие DATA/CHUNKS от сборки, а не плейсхолдеры `template3.html` —
  решило вопрос "extracted vs build-на-лету" сам собой: читать готовый
  собранный файл). Один тест-блок (18, работа с реальным корпусом слов)
  опирается на `sentences_final.json`, который специально в `.gitignore`
  (сырые скрейп-данные, только локально) — на свежем клоне без этого
  файла харнесс не падает, а корректно пропускает именно этот блок
  (проверено вживую: 56/56 без файла, 66/66 с файлом).

## Пусто — и это нормально

Если раздел выше пуст, значит либо всё сделано, либо ничего конкретного
ещё не предлагали — не значит, что писать сюда нечего в принципе.
