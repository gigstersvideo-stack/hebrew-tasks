# Курс "Корни" — 600 корней с теорией, практикой и растущими текстами

Дизайн-документ от 2026-09-11. Автор концепции — пользователь; структура
теории и разбор рисков — Claude. Статус: **обсуждается, не начато**.

## Идея в одном абзаце

600 самых частотных корней иврита, по частотности, разбиты на 3 базы по
200 ("1ые 200 корней", "2ые 200 корней", "3ие 200 корней"). Для каждого
корня — страница теории (разбор нейронкой, структура ниже) и 20 частотных
предложений с словами этого корня. Отдельная страница ведёт пользователя
по циклу теория → 20 предложений → теория → 20 предложений, корни
открываются строго по порядку; когда предложения корня пройдены, они
добавляются/открываются в соответствующей базе. Каждые 3 пройденных
корня — сгенерированный текст, использующий в основном уже изученные
корни. Часть текстов, которые получаются похожими на настоящие книги,
добавляются в библиотеку читалки **вручную** (автоматический критерий
"похоже на книгу" решили не строить — небыстро, не окупается).

**Решение по объёму (2026-09-11, после обсуждения рисков)**: начинаем
только с первой базы (200 самых частотных корней) — там живёт основная
польза по ресёрчу (50-100 корней уже дают большой прирост понимания, 600
— за гранью окупаемости на старте). Базы 2 и 3 — на будущее, архитектура
их не исключает, но контент под них не генерируем сейчас.

## Как базы встраиваются в существующую архитектуру

Три базы корней — это **обычные уровни в существующей системе пулов**
тренажёра (`progress.pool.levels`/`include`/`exclude`, тот же механизм,
что уже переключает алеф/бет/гимель) — не новая подсистема хранения.
Каждая база **всегда может быть выключена** пользователем тем же
переключателем уровня, что и обычные уровни сейчас. Предложения корня
физически появляются в пуле (через `appendCustomCards`-подобный
append-only механизм — см. project_hebrew_trainer_login.md про то, почему
DATA-позиции никогда не переставляются) только после того, как
пользователь прошёл теорию И 20 предложений этого корня — до этого момента
предложения корня просто не существуют в его локальной базе.

## Пайплайн данных

1. **Частотный список корней** — тот же подход, что описан в MARKETING.md
   для рынка, только для данных: взять открытый частотный список слов
   (напр. teachmehebrew, 10000 слов) → прогнать через Gemini (тот же
   подход, что `5_gemini_pipeline.py`) → определить корень каждого слова →
   просуммировать частоты по корням → взять топ-200 (было топ-600, сузили
   вместе с объёмом всего курса). Реальная частотность, не список
   неизвестного происхождения.
2. **Теория по каждому корню** — см. структуру ниже, генерируется один раз
   на корень.
3. **20 предложений на корень** — частотные, естественные, с реально
   употребимыми производными этого корня (не гипотетическими формами по
   всем 7 биньянам сразу — см. "Риски" про Arad 2005).
4. **Растущие тексты** каждые 3 корня — отдельный, менее детерминированный
   генеративный шаг (см. "Риски" про то, почему это самое узкое место).

## Предлагаемая структура теории (черновик, на обсуждение)

Основана на находках ресёрча (см. предыдущее сообщение в чате): корень не
предсказывает значение на 100% по всем биньянам, поэтому теория должна
показывать РЕАЛЬНО употребимые производные, а не гипотетическую полную
таблицу.

1. **Корень + один тезис-интуиция** ("שׁ-מ-ר — вокруг идеи охраны,
   сохранения") — явно как интуиция-подсказка, не строгое правило.
2. **2-4 реальных, частотных производных слова** этого корня — только те
   биньяны, что реально употребимы для ЭТОГО корня (не все 7 гипотетически).
   На каждое: слово с огласовками, биньян, часть речи, перевод, один
   пример употребления.
3. **Честная пометка расхождений** — если какое-то производное значит
   что-то не по интуиции из пункта 1 (а такое бывает часто — см. Arad),
   явно это отметить, а не замалчивать.
4. **Мнемоника** (опционально) — переиспользовать существующий паттерн
   иллюстраций-подсказок из читалки, если применимо для абстрактных
   корней.
5. Переход к практике: "Теперь увидим этот корень в реальных
   предложениях."

## Страница курса (флоу)

Теория корня 1 → 20 предложений в тренажёре (пул этого корня) → теория
корня 2 → 20 предложений → теория корня 3 → 20 предложений → **текст**
(использует в основном корни 1-3) → теория корня 4 → ... Предложения
конкретного корня становятся доступны в базе тренажёра только после
прохождения его теории (гейтинг).

## Переток в читалку

Тексты, которые получаются похожими на настоящую историю (не просто
набор тренировочных фраз), добавляются в библиотеку читалки как обычная
книга — вручную, решение "похоже на книгу или нет" принимает человек, не
код. Естественное развитие механики "macaronic fade" (постепенное
замещение перевода на иврит), которая там уже есть.

## Риски (из обсуждения перед стартом, 2026-09-11)

- **Растущие тексты — самое узкое технически место.** Чем больше корней
  накопилось, тем сложнее модели писать естественный, связный текст,
  который "в основном" использует уже пройденные корни — реальный риск
  словарного, неестественного текста, который формально удовлетворяет
  условию. Та же ловушка "формально верно, реально плохо", что уже ловили
  в проекте на никуде/иллюстрациях/произношении — ожидать того же самого
  здесь и закладывать время на ручную проверку именно этих текстов.
- **QA на объёме.** Даже на суженных 200 корнях × 20 предложений = 4000
  новых предложений — это половина текущего корпуса тренажёра. Глазами
  всё не проверить, полагаться на точечные проверки + перенос уже
  наработанных QA-привычек (как `8_audio_qa.py` у читалки).
- **Дедуп с существующими 8239 предложениями тренажёра** — не
  координируется автоматически, риск пересечения лексики/контента.
  Решить перед генерацией: сверять при генерации, или принять пересечение
  как есть.
- **Состояние гейтинга (какая теория пройдена) — новое состояние**,
  которого пока нет ни в localStorage, ни в схеме Firestore. Нужно
  спроектировать sync по аналогии с уже существующими полями прогресса
  (`progress.pool`/`meta`), не как отдельную систему.
- **Дневная квота Gemini — подтверждено вживую, не гипотеза.** Прогон
  `build_root_frequency_list.py` (2026-09-11) упёрся в лимит free tier
  `gemini-3.6-flash` — **20 запросов/сутки** — на 320 из 650 слов (после
  увеличения батча до 150 слов/запрос это ~5 запросов на весь список, то
  есть должно укладываться в сутки; при исходном батче 40 не влезло).
  **Обход найден и сработал в тот же день**: квота — per-model, не
  общая на ключ (видно прямо в тексте ошибки: `...PerProjectPerModel...`).
  Проверили вживую — на `gemini-flash-latest`, `gemini-flash-lite-latest`,
  `gemini-3-flash-preview`, `gemini-3.1-flash-lite`, `gemini-3.5-flash`,
  `gemini-3.5-flash-lite`, `gemini-3.7-flash`, `gemini-3.8-flash`,
  `gemma-4-26b-a4b-it` — у всех была отдельная свободная квота, когда
  `gemini-3.6-flash` уже была исчерпана. Дообработали оставшиеся 330 слов
  на `gemini-3.5-flash` в тот же день без ожидания сброса. Для теории и
  предложений (кратно больше запросов) держать это в уме — можно
  чередовать модели, если упрёмся снова, не обязательно ждать сутки.

## Шаг 1 — ✓ ГОТОВО (2026-09-11): реальный частотный список корней

- `hebrew_word_frequency_1_650.json` — 650 реальных слов современного
  иврита, ранжированных по частоте (teachmehebrew.com/hebrew-frequency-list.html,
  верхняя часть их списка на 10000 слов).
- `build_root_frequency_list.py` — питон-скрипт по образцу
  `5_gemini_pipeline.py`, прогнан вживую (ключ пользователя, модели
  `gemini-3.6-flash` + `gemini-3.5-flash` — см. "Риски" про квоту).
- `root_frequency.json` — **итоговый результат: 227 уникальных корней**
  (больше нужных 200!), отсортированы по частотно-взвешенному скору
  (сумма 1/rank по словам-членам), с реальными словами-примерами на
  каждый корень и их рангом в исходном списке. Качество на вид хорошее
  при точечной проверке топ-30 (ה-י-ה, ש-נ-ה, י-ת-ר, א-ח-ר и т.д. — все
  осмысленные, частотные, с настоящими производными).
- `root_frequency_raw.json` — промежуточный результат (слово → корень,
  до агрегации), на случай если понадобится переагрегировать по-другому.

## Шаг 2 — ✓ ГОТОВО (2026-09-12): промпт теории, дизайн страницы

- `generate_root_theory.py` — финальная схема (после нескольких раундов
  правок по вашим замечаниям): `semantic_field`, `origin_note`,
  `girzah_note` (тип גזרה + конкретика спряжения), `fun_fact`,
  `modern_usage_note`/`place_or_name_note` (объекты `{text, example_he,
  example_ru}` — у обоих теперь обязателен живой пример при непустом
  тексте), `common_collocation`, `song_or_quote_note`,
  `comprehension_question`, 2-4 `derived_words` с примерами,
  `set_phrases` (тоже с обязательными `example_he`/`example_ru`),
  `common_mistakes`, `divergence_note`.
- **Жёсткая автоматическая проверка на כתיב מלא** — `find_ktiv_chaser_violations()`
  рекурсивно сканирует весь JSON-ответ на два самых частых и однозначных
  бага неполного написания (холам без вав — חֹק вместо חוֹק; кубуц вместо
  шурук), встроена в retry-цикл наравне с ошибками API. Есть allowlist
  служебных слов, где холам без вав — норма (לֹא, כֹּה, זֹאת и формы с
  приставками ו/ה/ב/כ/ל/מ/ש — проверяется по буквенному суффиксу, не
  только точным словом). Протестирована и на синтетических кейсах, и
  вживую — поймала реальный баг (זֹאת с приставкой) на первом же прогоне
  с реальными данными, после чего allowlist доработан.
- Дизайн страницы теории обкатан на мокапе (`root_theory_design.html` в
  scratchpad) с реальным контентом ק-ו-ם, затем **встроен как экран в
  сам тренажёр** (`template3.html`: новый view `root-theory-view`,
  функция `renderRootTheory()`, данные в `ROOT_THEORY_DATA`, кнопка
  «📖 Курс «Корни»» на дашборде) — проверено вживую в браузере на
  собранной версии с реальными данными тренажёра, ошибок нет. Пока в
  массиве данных только один корень (ק-ו-ם) — заполняется по мере
  прогона пайплайна на остальные.

## Шаг 3 — ✓ ГОТОВО (2026-09-12): промпт для 20 предложений, дедуп решён

- `generate_root_sentences.py` — 20 предложений на корень, по
  производным словам из уже готовой теории (не вводит новую лексику),
  примерно поровну распределены по производным, с `cloze_token` —
  точной формой слова в предложении для упражнения на пропуск.
- **Решение по дедупу** (было открытым вопросом): корпус тренажёра
  (`sentences_final.json`, 8239 предложений) хранится БЕЗ огласовок,
  новые предложения курса — С, так что точное сравнение строк никогда
  не сработало бы. Сравнение идёт после снятия огласовок
  (`strip_niqud`, по Unicode-категории Mn) — точный дубль по содержанию
  теперь жёсткая проверка (как и כתיב מלא, тот же retry-цикл), а просто
  похожий текст (пересечение слов ≥70%) — мягкое предупреждение в лог,
  не блокирует генерацию.
- **Прогнано вживую на ק-ו-ם** (2026-09-12, `gemini-3.5-flash`, ключ
  пользователя): 20/20 предложений с первого чистого прогона после
  фикса allowlist, 0 нарушений כתיב מלא, 0 дублей с корпусом. Результат:
  `sentences_v1_kum.json`.

## Шаг 5 — ✓ ГОТОВО (2026-09-12): гейтинг + sync, реальное прохождение

- **Новый уровень в существующей системе пулов** — `ROOTS_BASE1_LEVEL_CODE`,
  обычная запись в `LEVELS` (символ 🌳), а не отдельная подсистема:
  включается/выключается тем же чекбоксом в «Мои базы», что и уровни
  א-ג — проверено вживую (выключение чекбокса реально убирает карточки
  из пула через тот же `inPool()`).
  Код уровня выделен ДО `CUSTOM_LEVEL_CODE` — иначе не попал бы ни в
  выпадающий фильтр, ни в список переключателей (оба строятся по
  `LEVELS` раньше, чем туда добавляется «🏷 Личное»).
- **Гейтинг** — `appendRootSentences()`/`markRootTheoryDone()`: те же
  append-only принципы, что у `appendCustomCards` (не переставлять
  DATA-позиции). Кнопка «Начать: 20 предложений» разблокируется только
  после клика по любому варианту в `comprehension_question` (не после
  просто открытия страницы) и по клику реально добавляет 20 предложений
  в конец `DATA`, включает уровень в `progress.pool.levels`, ведёт на
  дашборд. Повторный визит на уже пройденный корень показывает
  состояние "✓ Пройдено" вместо повторной разблокировки.
- **Sync**: `progress.rootsCourse.completedTheory` (список id корней,
  устройство-независимый) уходит в Firestore через `metaPayload()`/
  `pullAndMerge()` как обычное поле меты — СЛИВАЕТСЯ (не перезаписывается),
  как и на другом устройстве могло разблокироваться в другом порядке.
  `sentenceIndexByRoot` (сырые DATA-индексы) намеренно НЕ синхронизируется
  — это позиции конкретного устройства; вместо этого при каждой загрузке
  страницы предложения всех пройденных корней заново раскладываются в
  конец свежего `DATA` в том же порядке (`completedTheory` меняется
  только через push в конец — порядок стабилен).
- **Реальный баг, пойманный тестированием перезагрузки** (не гипотетический):
  первая версия гвардила "уже разложено" по `progress.rootsCourse.sentenceIndexByRoot`
  (переживает перезагрузку) вместо состояния ЭТОЙ загрузки страницы — после
  reload `progress` утверждал, что индексы 8239-8258 существуют, а
  `DATA` (пересобранный с нуля) на самом деле их не содержал. Поймано
  вживую (`DATA[8239]` был `undefined` после reload), исправлено
  отдельным `Set`, не переживающим перезагрузку. Перепроверено: 3
  перезагрузки подряд дают одни и те же индексы, `DATA.length` растёт
  ровно один раз, карточка реально проигрывается в настоящем экране
  повторения (RU→HE, с бейджем уровня «🌳 · Корни · база 1»).

## Следующие шаги

1. ~~Прогнать скрипт, посмотреть на реальный топ~~ — сделано.
2. ~~Промпт для теории~~ — сделано, см. выше.
3. ~~Промпт для 20 предложений, дедуп~~ — сделано, см. выше.
5. ~~Гейтинг/sync, реальное прохождение~~ — сделано, см. выше.
4. **Прогнать оба скрипта на остальных ~199 корнях базы 1** —
   единственное, что осталось перед полным курсом. Займёт несколько
   дней из-за дневной квоты Gemini (20 запросов/сутки на модель,
   обходится ротацией моделей — см. "Риски" выше), но технических
   преград больше нет вообще: промпты, проверки, встраивание в
   тренажёр, гейтинг и sync — всё обкатано вживую на ק-ו-ם. Просто
   добавлять новые записи в `ROOT_THEORY_DATA`/`ROOT_SENTENCES_DATA` по
   мере прогона.
